import * as allValidators from "./validators";
import * as allFormatters from "./formatters";

export default {
  validators: {
    ...allValidators,
  },
  formatters: {
    ...allFormatters,
  },
};
